#import json

# Use a relative path if the JSON file is in the parent directory
#with open("../articles.json", "r", encoding="utf-8") as file:
 #   articles_data = json.load(file)
#    for article in articles_data:
  #      print(article)  # Print each article to verify
